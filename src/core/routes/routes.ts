interface SwitchRoutes {
  home: string;
  accounts: string;
  cards: string;
  transactions: string;
  balance: string;
}

export const switchRoutes: SwitchRoutes = {
  home: "/home",
  accounts: "/accounts",
  cards: "/cards",
  transactions: "/transactions",
  balance: "/balance",
};
